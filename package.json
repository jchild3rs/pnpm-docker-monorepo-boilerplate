{
  "name": "pnpm-docker-monorepo-boilerplate",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --no-color --output-logs=new-only",
    "test": "turbo run test --no-color --output-logs=new-only",
    "docker": "DOCKER_BUILDKIT=1 turbo run docker --no-color --output-logs=new-only",
    "dev": "turbo run dev --parallel --no-cache",
    "lint": "turbo run lint --no-color --output-logs=new-only",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@6.32.9",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "turbo": "latest"
  }
}
