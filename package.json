{
  "name": "pnpm-docker-monorepo-boilerplate",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "docker": "DOCKER_BUILDKIT=1 turbo run docker --output-logs=new-only",
    "publish": "DOCKER_BUILDKIT=1 turbo run publish --output-logs=new-only",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "release": "changeset publish"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@6.32.9",
  "devDependencies": {
		"@changesets/cli": "^2.22.0",
    "rimraf": "^3.0.2",
    "turbo": "latest"
  }
}
